name: CD-8-Guardian-Secretos
on: [workflow_dispatch]
jobs:
  secure-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inyectar Secreto
        run: echo "Configurando conexión segura..."
        env:
          DB_PASSWORD: ${{ secrets.DB_PROD_PASSWORD }} # Definido en Settings > Secrets 